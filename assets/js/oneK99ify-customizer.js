;( function( $ ) {

	console.log( 'wp' );



	// wp.customize( 'onek99ify_mod[mouse_trail_status]', function( val ) {

	// 	console.log( val );

	// 	val.bind( function( newval ) {

	// 		console.log( newval );

	// 		// $('body').css('background-color', newval );
	// 	} );

	// } );


	// wp.customize.bind( 'ready', function() {
		// console.log( wp.customize.control( 'onek99ify_ctrl_mouse_trail_status' ) );
// 		wp.customize.control( 'oneonek99ify_mouse_trail_status', function ( control ) {
// console.log( control );
// 			control.setting.bind( function ( val ) {

// 				console.log( val );

// 			} );

// 		} );

	// } );

} )( jQuery );
